{
  "name": "reapp-routes",
  "version": "0.10.14",
  "description": "helper for consistently requiring routes to filenames",
  "main": "index.js",
  "scripts": {
    "build": "babel src --out-dir . --stage=0",
    "watch": "babel src --out-dir . --stage=0 --watch"
  },
  "repository": {
    "type": "git",
    "url": "github.com/reapp/reapp-route-tree"
  },
  "author": "Nate Wienert",
  "license": "MIT",
  "dependencies": {
    "react": "0.14.x",
    "react-dom": "^0.14.0",
    "reapp-platform": "*",
    "react-router": "0.13.x",
    "reapp-object-assign": "^1.0.0"
  },
  "bugs": {
    "url": "https://github.com/reapp/reapp-routes/issues"
  },
  "homepage": "https://github.com/reapp/reapp-routes",
  "devDependencies": {
    "babel": "^5.0.0"
  }
}
